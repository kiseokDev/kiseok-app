import { Button, Text, View } from "react-native";

export default function SettingIndex() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Setting</Text>
    </View>
  );
}
